#!/bin/sh

set -xe

source /configs/vars

if [ "${RR_PLATFORM}" != "surfacert" ]; then
  exit 0;
fi

echo "rr-run-parts: $(basename $0)"

#ln -s /usr/lib/libidn.so /usr/lib/libidn.so.11
#mkinitcpio -p linux

# TODO: fixme
# disable services
#source /configs/services
#systemctl disable ${RR_SERVICES}

